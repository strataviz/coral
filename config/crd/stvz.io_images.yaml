---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: images.stvz.io
spec:
  group: stvz.io
  names:
    kind: Image
    listKind: ImageList
    plural: images
    shortNames:
    - img
    singular: images
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The number of total images managed by the object
      jsonPath: .status.totalImages
      name: Images
      type: integer
    - description: The number of images that are currently available on the nodes
      jsonPath: .status.availableImages
      name: Available
      type: integer
    - description: The number of images that are currently pending on the nodes
      jsonPath: .status.pendingImages
      name: Pending
      type: integer
    - description: The number of images that are currently pending deletion on the
        nodes
      jsonPath: .status.deletingImages
      name: Deleting
      type: integer
    - description: The number of images that are in an unknown state on the nodes
      jsonPath: .status.unknownImages
      name: Unknown
      priority: 1
      type: integer
    - description: The number of nodes matching the selector (if any)
      jsonPath: .status.totalNodes
      name: Nodes
      priority: 1
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              enabled:
                type: boolean
              imagePullSecrets:
                items:
                  properties:
                    name:
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                nullable: true
                type: array
              images:
                items:
                  properties:
                    name:
                      type: string
                    tags:
                      items:
                        type: string
                      maxItems: 100
                      minItems: 1
                      type: array
                  required:
                  - name
                  - tags
                  type: object
                type: array
              pollInterval:
                type: string
              selector:
                items:
                  properties:
                    key:
                      type: string
                    operator:
                      type: string
                    values:
                      items:
                        type: string
                      type: array
                  required:
                  - key
                  - operator
                  - values
                  type: object
                nullable: true
                type: array
            type: object
          status:
            properties:
              availableImages:
                type: integer
              deletingImages:
                type: integer
              pendingImages:
                type: integer
              totalImages:
                type: integer
              totalNodes:
                type: integer
              unknownImages:
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
